language: java

sudo: required

jdk:
  - oraclejdk8
  - oraclejdk7

install: true

script:
  - sudo apt-get update && sudo apt-get install oracle-java8-installer
  - java -version
  
  
  
  
before_install:
  - pip install codecov
after_success:
  - codecov
